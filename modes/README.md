# modes 视觉模式系统

这里保存“业务场景到参数配置”的映射。

建议内容：

- 模式注册代码：`mode_registry.py`
- 可执行模式配置：化妆品、食品、建筑、电影感、PPT商业、AI商业KV
- 每个模式对应目标质感、适用素材、禁用操作、输出命名规则

原则：

- 模式只描述视觉目标和参数，不直接写底层图像算法。
- 同一算法可以被多个模式复用。
- 新模式先加配置，再决定是否需要新增算法。
