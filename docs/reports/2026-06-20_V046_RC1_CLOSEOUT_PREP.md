# V0.4.6 RC1 工程收口准备清单

生成时间：2026-06-20  
当前结论：`CONDITIONAL_READY_FOR_RC1_CLOSEOUT_PREP / NOT_FROZEN / EVIDENCE_COMPLETE`  
当前 HEAD：`111393be65107b0e7670dd70a62681424a9f09a7`

## 收口原则

本文件只列 RC1 工程收口准备项，不执行提交、不冻结、不接正式生产链。

## 必须执行的工程回归

|项目|目标|当前状态|
|---|---|---|
|后端语法检查|确认 Python 文件无语法错误|待执行|
|前端 build|确认 RC1 前台可构建|待执行|
|API 上传回归|JPG / PNG / 透明 PNG / 中文小字上传均可完成|待执行|
|任务轮询回归|上传后无需 SSE 也能完成任务|待执行|
|SSE 回归|成功、失败、重连、双订阅均不重复处理|待执行|
|final_output_url 回归|URL 指向真实最终文件，不暴露本地盘符作为图片 src|待执行|
|文件落盘回归|output_dir、final 文件、诊断文件路径稳定|待执行|
|诊断 ZIP 回归|不包含原图和最终输出，脱敏字段正确|待执行|
|输出目录与路径索引|逻辑路径、物理路径、平台类型、file_exists、file_size 完整|待执行|
|交付状态映射|PASS / PASS_WITH_LIMITATION / FAIL 前后端一致|待执行|

## 画质与交付状态门禁

必须继续保留：

- `PASS` → 可交付。
- `PASS_WITH_LIMITATION` → 建议人工复核。
- `FAIL` → 不建议交付。

不得把 `PASS_WITH_LIMITATION` 合并为普通成功。

## 当前 19 张黄金集收口状态

|项目|结果|
|---|---:|
|19 张完成|19/19|
|POSITIVE|2|
|MINOR_POSITIVE|4|
|NEUTRAL|13|
|REJECT|0|
|PASS_WITH_LIMITATION|10|
|证据缺失|0|
|Alpha 失败|0|
|高光兜底误触发|0|

## 当前允许动作

- 允许进入 RC1 工程收口准备。
- 允许执行后端语法检查、前端 build、API/SSE/任务轮询/final_output_url/诊断 ZIP smoke。
- 允许整理提交范围。
- 允许让 ChatGPT / Gemini 做视觉与交付口径复核。

## 当前禁止动作

- 不允许冻结。
- 不允许提交。
- 不允许接正式生产链。
- 不允许进入 2K / 4K。
- 不允许新增模式或扩 UI。
- 不允许把 19 张回归结果写成商业最终通过。

## 建议的下一步执行顺序

1. 完成 ChatGPT 对 19 张点检报告与收口清单的复核。
2. 运行后端语法检查。
3. 运行前端生产 build。
4. 运行 API / SSE / 任务轮询 / final_output_url / 诊断 ZIP smoke。
5. 核对当前 Git diff scope，把应提交文件和不应提交文件分开。
6. 生成 RC1 closeout 最终报告。
7. 经用户确认后再提交。

## 仍需保留为已知限制的问题

- `PASS_WITH_LIMITATION=10`，其中多张由体积收益比、质量门未满或保护策略触发。
- `synthetic_gradient_band` 体积 ratio 极高，必须保留人工复核提示。
- 文字密集图仍不能按“明显提升”表达，只能按保护或复核表达。
- 品牌色、渐变、高光样本不能只按分数判断增强收益。
- 部分输出文件体积增长明显，RC1 前台必须把风险原因展示清楚。
- 当前 Round 2.3 高光兜底在 19 张中没有触发，说明没有误触发，但 Andy 专项仍应保留独立人工复核结论。

## RC1 收口准入判断

当前允许进入：`RC1 工程收口准备`。  
当前不允许进入：`冻结 / 提交 / 正式生产链接入`。
